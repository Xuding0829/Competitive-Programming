#include <bits/stdc++.h>

using namespace std;

const int N = 2e5 + 100;
int a[N];
int n, m;

signed main()
{
    int a, b, h;
    cin >> a >> b >> h;
    cout << (a + b) * h * 1.0 / 2 << endl;
    return 0;
}